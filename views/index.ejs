<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta
			name="viewport"
			content="width=device-width, initial-scale=1.0" />
		<link
			rel="stylesheet"
			href="/styles/style.css" />
		<script src="https://unpkg.com/@tailwindcss/browser@4"></script>
		<title>Document</title>
	</head>
	<body>
		<div class="main w-full min-h-screen">
			<div class="form p-10">
				<form
					action="/create"
					method="post">
					<input
						class="py-2 px-4 rounded block outline-none w-full bg-gray-100"
						type="text"
						name="title"
						placeholder="Title goes here" />
					<textarea
						name="details"
						class="mt-4 py-2 px-4 resize-none outline-none w-full rounded block bg-gray-100"
						placeholder="Write your task details here"></textarea>

					<input
						class="px-4 py-2 mt-4 bg-blue-500 text-white rounded hover:bg-blue-700"
						type="submit"
						value="Create Task" />
				</form>
			</div>
			<div class="tasks flex gap-3 flex-wrap p-10">
				<% if(files.length > 0) { %> <% files.forEach(function(val){ %>
				<div class="task py-4 px-3 bg-gray-100 min-w-72 rounded">
					<h1 class="text-2xl tracking-tighter"><%=val%></h1>
					<div class="buttons flex items-center justify-between mt-5">
						<a
							href="/file/<%= val %>"
							class="text-blue-400 inline-block hover:underline"
							>Read More</a
						>
						<a
							href="/edit/<%= val %>"
							class="text-gray-400 inline-block hover:underline"
							>Edit filename</a
						>
					</div>
				</div>
				<% }) %> <%}else {%>
				<h3>No Tasks</h3>
				<%}%>
			</div>
		</div>

		<script src="/javascripts/script.js"></script>
	</body>
</html>
